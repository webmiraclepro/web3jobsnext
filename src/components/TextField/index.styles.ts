import { styled, TextField, Box } from '@mui/material';

export const AppTextFieldContainer = styled(Box)({
  width: '100%',
  display: 'flex',
  flexDirection: 'column',
});

export const CustomTextField = styled(TextField)({});
